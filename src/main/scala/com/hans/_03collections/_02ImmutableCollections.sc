import scala.collection.immutable.SortedSet
import scala.collection.mutable
import scala.collection.immutable

val a = Set(1, 2, 3)

val b = a + 4

val sSet = SortedSet(3, 2, 4, 1, 9)

val toMutableSet = a.to(mutable.Set)
val toImmutableSet = toMutableSet to immutable.Set



List(2, 3, 4)

Map(1 -> "A", 2 -> "B")



